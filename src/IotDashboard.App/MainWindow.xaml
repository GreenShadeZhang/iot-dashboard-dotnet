<Window
    x:Class="IotDashboard.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IotDashboard.App"
    xmlns:viewmodels="using:IotDashboard.App.ViewModels"
    Title="IoT Dashboard">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE968;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                <TextBlock Text="IoT Device Dashboard" 
                          VerticalAlignment="Center"
                          Style="{ThemeResource TitleTextBlockStyle}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <ComboBox x:Name="PortComboBox"
                         PlaceholderText="Select Port"
                         ItemsSource="{x:Bind ViewModel.AvailablePorts}"
                         SelectedItem="{x:Bind ViewModel.SelectedPort, Mode=TwoWay}"
                         Width="120"
                         IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                
                <Button Content="Refresh" 
                       Command="{x:Bind ViewModel.RefreshPortsCommand}"
                       IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                
                <Button x:Name="ConnectButton"
                       Content="{x:Bind ViewModel.IsConnected, Mode=OneWay, Converter={StaticResource ConnectButtonTextConverter}}"
                       Command="{x:Bind ViewModel.ConnectCommand}"
                       Style="{ThemeResource AccentButtonStyle}"/>
            </StackPanel>
        </Grid>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sensor Data Card -->
                <Border Grid.Row="0" Grid.Column="0" 
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="16"
                       Margin="0,0,8,16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Environmental Sensors" 
                                  Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7C1;" FontSize="20" Foreground="#FF5722"/>
                                <TextBlock Text="Temperature" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.Temperature, Mode=OneWay}" 
                                          Style="{ThemeResource TitleLargeTextBlockStyle}"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                <TextBlock Text="°C" 
                                          VerticalAlignment="Bottom"
                                          Margin="0,0,0,4"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE753;" FontSize="20" Foreground="#2196F3"/>
                                <TextBlock Text="Humidity" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.Humidity, Mode=OneWay}" 
                                          Style="{ThemeResource TitleLargeTextBlockStyle}"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                                <TextBlock Text="%" 
                                          VerticalAlignment="Bottom"
                                          Margin="0,0,0,4"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Device Status Card -->
                <Border Grid.Row="0" Grid.Column="1" 
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="16"
                       Margin="8,0,0,16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Device Status" 
                                  Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        
                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse Width="12" Height="12" 
                                        Fill="{x:Bind ViewModel.IsOnline, Mode=OneWay, Converter={StaticResource OnlineStatusColorConverter}}"
                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Bind ViewModel.IsOnline, Mode=OneWay, Converter={StaticResource OnlineStatusTextConverter}}" 
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE83F;" FontSize="20"/>
                                <TextBlock Text="Battery Level" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ProgressBar Value="{x:Bind ViewModel.BatteryLevel, Mode=OneWay}" 
                                        Maximum="100"
                                        Foreground="{x:Bind ViewModel.BatteryLevel, Mode=OneWay, Converter={StaticResource BatteryColorConverter}}"/>
                            <TextBlock Text="{x:Bind ViewModel.BatteryLevel, Mode=OneWay, Converter={StaticResource BatteryLevelTextConverter}}" 
                                      Style="{ThemeResource CaptionTextBlockStyle}"/>
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Text="Last Update" 
                                      Style="{ThemeResource CaptionTextBlockStyle}"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.LastUpdate, Mode=OneWay}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Light Control Card -->
                <Border Grid.Row="1" Grid.Column="0" 
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="16"
                       Margin="0,0,8,16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Light Control" 
                                  Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <FontIcon Glyph="{x:Bind ViewModel.LightOn, Mode=OneWay, Converter={StaticResource LightIconConverter}}" 
                                         FontSize="48"
                                         Foreground="{x:Bind ViewModel.LightOn, Mode=OneWay, Converter={StaticResource LightColorConverter}}"/>
                                <StackPanel VerticalAlignment="Center" Spacing="4">
                                    <TextBlock Text="{x:Bind ViewModel.LightOn, Mode=OneWay, Converter={StaticResource LightStatusTextConverter}}" 
                                              Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="Smart Light" 
                                              Style="{ThemeResource CaptionTextBlockStyle}"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <ToggleButton x:Name="LightToggle"
                                         Content="{x:Bind ViewModel.LightOn, Mode=OneWay, Converter={StaticResource LightButtonTextConverter}}"
                                         IsChecked="{x:Bind ViewModel.LightOn, Mode=OneWay}"
                                         Command="{x:Bind ViewModel.ToggleLightCommand}"
                                         IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}"
                                         HorizontalAlignment="Stretch"
                                         Style="{ThemeResource AccentButtonStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Fan Control Card -->
                <Border Grid.Row="1" Grid.Column="1" 
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="16"
                       Margin="8,0,0,16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Fan Control" 
                                  Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        
                        <StackPanel Spacing="12">
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <FontIcon Glyph="&#xE90C;" 
                                         FontSize="48"
                                         Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <StackPanel VerticalAlignment="Center" Spacing="4">
                                    <TextBlock Text="{x:Bind ViewModel.FanSpeed, Mode=OneWay, Converter={StaticResource FanSpeedTextConverter}}" 
                                              Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                                    <TextBlock Text="Smart Fan" 
                                              Style="{ThemeResource CaptionTextBlockStyle}"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <Slider x:Name="FanSpeedSlider"
                                   Header="Speed"
                                   Minimum="0"
                                   Maximum="100"
                                   Value="{x:Bind ViewModel.FanSpeed, Mode=TwoWay}"
                                   IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}"
                                   ThumbToolTipValueConverter="{StaticResource FanSpeedTooltipConverter}"/>
                            
                            <Button Content="Apply Speed"
                                   Command="{x:Bind ViewModel.UpdateFanSpeedCommand}"
                                   IsEnabled="{x:Bind ViewModel.IsConnected, Mode=OneWay}"
                                   HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
               Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
               BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
               BorderThickness="0,1,0,0"
               Padding="16,8">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <FontIcon Glyph="&#xE946;" FontSize="16" VerticalAlignment="Center"/>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" 
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
